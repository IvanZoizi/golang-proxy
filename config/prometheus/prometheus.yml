global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  - job_name: "proxy"
    static_configs:
      - targets: ['proxy:8080']
    scrape_interval: 10s
    bearer_token: 'secret-key'
